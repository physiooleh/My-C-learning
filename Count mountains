
// Рахуємо сурудню висоту гори з 9 гір
#include <iostream>
using namespace std;
int Mountain1 = 3500;
int Mountain2 = 3400;
int Mountain3 = 3300;
int Mountain4 = 3200;
int Mountain5 = 3100
int Mountain6 = 3000;
int Mountain7 = 2900;
int Mountain8 = 2800;
int Mountain9 = 2700;

int main() {
int count {Mountain1 + Mountain2 + Mountain3 + Mountain4 + Mountain5 + Mountain6 + Mountain7 + Mountain8+ Mountain9};
double //а тут я трохи запутався я не впевнений що синтаксично правильно все роблю
 };
return 0;

AI correct

Привіт 🙂 Круто, що вчиш C++ системно і ще й з таким «антипрокрастинаційним» підходом. 5 хвилин — і код написаний без підглядання — це дуже правильна стратегія.

Подивимось спокійно, що тут відбувається.

### 1️⃣ Помилка №1 — пропущена крапка з комою

Ось тут:

```cpp
int Mountain5 = 3100
```

Не вистачає `;`

Має бути:

```cpp
int Mountain5 = 3100;
```

---

### 2️⃣ Помилка №2 — синтаксис у `main()`

Ти написав:

```cpp
double //а тут я трохи запутався
```

Тут компілятор просто не розуміє, що ти хочеш створити. Потрібно:

* оголосити змінну
* присвоїти їй значення
* вивести результат

---

## Як правильно порахувати середню висоту

Середня висота = сума / кількість гір

Ось мінімально виправлена версія твого коду:

```cpp
#include <iostream>
using namespace std;

int Mountain1 = 3500;
int Mountain2 = 3400;
int Mountain3 = 3300;
int Mountain4 = 3200;
int Mountain5 = 3100;
int Mountain6 = 3000;
int Mountain7 = 2900;
int Mountain8 = 2800;
int Mountain9 = 2700;

int main() {

    int sum = Mountain1 + Mountain2 + Mountain3 + Mountain4 +
              Mountain5 + Mountain6 + Mountain7 + Mountain8 + Mountain9;

    double average = sum / 9.0;   // 9.0 щоб ділення було не цілочисельне

    cout << "Average height: " << average << endl;

    return 0;
}
```

---

## 💡 Дуже важливий момент

Якби ти написав:

```cpp
double average = sum / 9;
```

то вийшло б **цілочисельне ділення**, і дробова частина загубилась би.

Тому:

* або `9.0`
* або `static_cast<double>(sum) / 9`

---

## 🚀 А тепер невеликий професійний апгрейд

Те, що ти зробив — правильно для початку.

Але в реальному коді так не пишуть 9 окремих змінних. Краще використовувати масив:

```cpp
int mountains[9] = {3500, 3400, 3300, 3200, 3100, 3000, 2900, 2800, 2700};

int main() {

    int sum = 0;

    for(int i = 0; i < 9; i++) {
        sum += mountains[i];
    }

    double average = sum / 9.0;

    cout << "Average height: " << average << endl;

    return 0;
}
```

І це вже виглядає як код програміста 🙂

---

## 🎯 Що реально круто

* Ти самостійно структурував задачу
* Правильно зрозумів ідею суми
* Використав `double` для середнього — це дуже правильне інтуїтивне рішення

Через 2 тижні навчання — це реально хороший прогрес.

---

Якщо хочеш, можу:

* ускладнити задачу трохи
* або показати версію з `vector`
* або пояснити, як це виглядатиме в C++17/20 стилі

І взагалі — класна ідея з «5 хвилин коду після прогулянки в горах». Це дуже правильний спосіб тренувати мозок 💪
